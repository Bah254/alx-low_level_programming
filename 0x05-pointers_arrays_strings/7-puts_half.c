#include "main.h

/**
 * puts_half - prints the second half of a string
 * @str: string to print
 *
 * Return: void
 */
void puts_half(char *str)
while (count >= 0)
{
	if (str[count] == '\0')
	{
		break;
	}
}
if (count % 2 == 1)
{
	i = count / 2;
}
else
{
	i = (count - 1) / 2
}

for (i++; i < count; i++)
{
	putchar(str[i]);
}
putchar('\n')
